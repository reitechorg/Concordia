version: '3'

interval: '500ms'

tasks:
    ts-build:
        internal: true
        sources:
            - ./src/**/*.ts
        cmds:
            - pnpm build-typescript

    dev:
        desc: Run dev mode on client
        deps: [await-port]
        cmds:
            - pnpm dev
        env:
            NODE_ENV: development

    build:
        deps: [ts-build]
        env:
            NODE_ENV: production

    install:
        cmds:
            - pnpm install

    await-port:
        internal: true
        cmds:
            - node ./scripts/await-port.js
