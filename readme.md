## Requirements:

- Node.js
- pnpm
- taskfile

## Setup

Install all npm dependencies:

```bash
task install
```

## Development

To start the development server for the client, run:

```bash
task dev
```

To start the development server for the server, run:

```bash
task server:dev
```
